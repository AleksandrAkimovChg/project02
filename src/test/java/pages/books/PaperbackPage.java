package pages.books;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.ProductCategoriesPage;

public class PaperbackPage extends ProductCategoriesPage {
    public PaperbackPage(WebDriver driver) {
        super(driver);
    }
}
