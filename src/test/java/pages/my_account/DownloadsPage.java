package pages.my_account;

import org.openqa.selenium.WebDriver;
import pages.ITable;
import pages.base_abstract.MainPage;

public class DownloadsPage extends MainPage implements ITable {
    public DownloadsPage(WebDriver driver) {
        super(driver);
    }
}
