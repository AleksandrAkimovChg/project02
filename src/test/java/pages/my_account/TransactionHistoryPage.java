package pages.my_account;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.TablePage;

public class TransactionHistoryPage extends TablePage {
    public TransactionHistoryPage(WebDriver driver) {
        super(driver);
    }
}
