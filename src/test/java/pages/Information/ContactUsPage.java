package pages.Information;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.FormPage;

public class ContactUsPage extends FormPage {
    public ContactUsPage(WebDriver driver) {
        super(driver);
    }
}
