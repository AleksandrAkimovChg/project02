package pages.home.account;

import org.openqa.selenium.WebDriver;

public class CreateAccountPage extends AccountLoginPage{
    public CreateAccountPage(WebDriver driver) {
        super(driver);
    }
}
