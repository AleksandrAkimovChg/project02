package pages.home.brands_scrolling_list;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.MainPage;

public class SephoraPage extends MainPage {
    public SephoraPage(WebDriver driver) {
        super(driver);
    }
}
