package pages.home.brands_scrolling_list;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.MainPage;

public class DovePage extends MainPage {
    public DovePage(WebDriver driver) {
        super(driver);
    }
}
