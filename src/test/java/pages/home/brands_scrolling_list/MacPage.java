package pages.home.brands_scrolling_list;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.MainPage;

public class MacPage extends MainPage {
    public MacPage(WebDriver driver) {
        super(driver);
    }
}
