package pages.home.cart;

import org.openqa.selenium.WebDriver;
import pages.home.CheckoutPage;

public class SuccessPage extends CheckoutPage {
    public SuccessPage(WebDriver driver) {
        super(driver);
    }
}
