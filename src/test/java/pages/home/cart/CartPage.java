package pages.home.cart;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.FormPage;

public class CartPage extends FormPage {
    public CartPage(WebDriver driver) {
        super(driver);
    }
}
