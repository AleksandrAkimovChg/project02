package pages.home;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.MainPage;

public abstract class HomeSubmenuPage extends MainPage {
    public HomeSubmenuPage(WebDriver driver) {
        super(driver);
    }
}
