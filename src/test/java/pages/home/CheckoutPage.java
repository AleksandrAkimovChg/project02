package pages.home;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.FormPage;

public class CheckoutPage extends FormPage {
    public CheckoutPage(WebDriver driver) {
        super(driver);
    }
}
